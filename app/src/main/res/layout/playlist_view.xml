<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/image"
        android:layout_width="@dimen/playlist_image_edge"
        android:layout_height="@dimen/playlist_image_edge"
        android:src="@drawable/ic_playlist_placeholder"
        android:scaleType="centerInside"
        android:layout_gravity="center" />

    <LinearLayout
        android:layout_width="160dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical">

            <TextView
                android:id="@+id/title"
                android:layout_marginTop="4dp"
                android:gravity="start"
                style="@style/PlaylistDescription"
                tools:text="@string/playlist_title"/>

            <TextView
                android:id="@+id/count"
                android:gravity="start"
                style="@style/PlaylistDescription"
                tools:text="@string/track_сount"/>

    </LinearLayout>

</LinearLayout>